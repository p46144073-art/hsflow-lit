## 分支規則（不可違反）

- 一律開分支，分支名用 `task/簡短描述`
- 完成後開 PR，**絕對不要自己合併 PR**
- 不要直接 commit 到 main，不要 force push，不要刪除任何分支
- PR 描述必須列出三件事：
  1. 改動了哪幾筆 LIBRARY 資料（列出 id）
  2. 動了哪些 LIBRARY 以外的程式碼
  3. 為什麼要這樣改
- 如果有任何自己判斷、使用者沒明講的決定，在 PR 描述裡單獨列出來
# 專案規則：高速流場文獻導引

## 這是什麼

單一檔案的靜態文獻導引網站，部署在 GitHub Pages。
**整站只有 `index.html` 一個檔**，CSS、JavaScript、文獻資料全部內嵌其中。
不要建立 `assets/` 資料夾、不要拆分檔案、不要引入建置工具或 npm 相依套件。

資料在 `<script>` 區塊頂端的 `LIBRARY` 陣列，標記行是：
`▼▼▼ 文獻資料庫：要新增文獻就改這裡（LIBRARY 陣列） ▼▼▼`

---

## 不可違反的規則

### 1. 絕對不要編造書目資料

這是本專案存在的理由。使用者是研究生，這些書目會進入論文的參考文獻。

- **不確定的卷、期、頁碼、年份，一律不要填。** 寧可留 `null` 或標 `unverified`
- 不要從記憶中「回想」DOI。DOI 必須來自實際查證
- 如果無法查證，就在 `note` 寫明「未查證」，並把 `biblioCheck` 設為 `"unverified"`
- 發現不同來源記載衝突時，設 `biblioCheck: "conflict"`，並在 `note` 寫清楚兩種說法與建議採用哪一版及理由

### 2. 絕對不要編造 `contribution` 或 `pages`

- `contribution` 與 `summary` 只有在**實際讀過原文或摘要**時才填。沒有就是 `null`
- `summary` 的第一行一定要交代來源。若內容來自使用者提供的轉述而非原文，就照實寫明，
  並且不要填 `pages`——沒有原文可對頁碼
- `pages` 只有在確認過該主張出現在該頁時才填。沒有就是 `null`
- **不要用二次文獻的轉述、不要用標題推測、不要用「這類論文通常會說」來填**
- 首頁的「關鍵主張附頁碼」百分比是誠實性指標，不准為了讓數字好看而灌水

### 3. 不要幫使用者「潤飾」追問句成摘要

`question` 欄位必須是**可證偽的問題**，不是內容摘要。
壞例子：「這篇探討了壓縮性效應」
好例子：「成長率下降是壓縮性造成的，還是厚度定義造成的？」

### 4. 動 `LIBRARY` 之後一定要驗語法

改完必須驗證 JS 可解析，例如：

```bash
python3 -c "
import re
h=open('index.html',encoding='utf-8').read()
js=re.findall(r'<script>(.*?)</script>', h, re.S)[0]
open('/tmp/x.js','w',encoding='utf-8').write(js)
"
node --check /tmp/x.js
```

沒有 node 時，至少確認：引號全為英文半形、每個物件結尾有 `,`、括號成對。

**中文全形引號「」是最常見的錯誤來源，絕對不要出現在程式碼中。**

---

## 資料格式

```js
{
  id: "A-24",              // 軌道代號 + 流水號，全站唯一。同時決定顯示順序，見下方說明
  track: "A",              // "A" 剪切層與凹槽 | "B" Taylor–Maccoll | "C" 錐流實驗與紋影
  year: 2021,              // 西元年數字（導軌範圍 1930–2030）
  method: "數值",          // 實驗 | 數值 | 理論 | 綜述 | 表格 | 教科書
  topic: "凹槽振盪",       // 自由字串，同名會自動合併成同一個篩選晶片
  title: "原文標題，不要翻譯",
  authors: "Surname, A.B.; Surname, C.D.",
  venue: "J. Fluid Mech. 912, A34",
  doi: "10.1017/jfm.2020.xxx",  // 純 DOI，無網址前綴；沒有填 null
  url: null,               // 選填。確定可取得全文的網址才填，卡片會顯示「全文」直達連結
  question: "可證偽的追問句",
  contribution: null,      // 未讀原文一律 null
  summary: null,           // 選填。摺疊式重點摘要，證據標準與 contribution 相同；
                           // 第一行必須寫明出處（自原文整理／使用者提供）
  pages: null,             // 未確認一律 null
  biblioCheck: "unverified",  // verified | unverified | conflict
  note: null               // 給使用者的備註
},
```

### id 決定顯示順序，不要隨便插號

網頁的卡片、側欄、序號一律**依 id 排序**（不是依年代）。id 本身就是編輯者排好的
閱讀路線，而且每組內部已經是年代遞增：

| 組 | id 範圍 | 年代 |
|---|---|---|
| A1 可壓縮剪切層 | A-01–A-11 | 1974 → 2002 |
| A2 凹槽流場：回饋振盪 | A-12–A-17 | 1964 → 2017 |
| A3 凹槽作為超燃衝壓穩焰器 | A-18–A-23 | 2001 → 2014 |
| B1 奠基 | B-01–B-03 | 1933 → 1937 |
| B2 表格化與數值解 | B-04–B-05 | 1947 → 1964 |
| B3 近似解與解析結構 | B-06–B-10 | 1967 → 2019 |
| B4 內收縮解與進氣道應用 | B-11–B-19 | 1966 → 2024 |
| B5 教科書（不計入篇數） | B-T1–B-T3 | 1957 → 1990 |
| C1 紋影與定量密度場 | C-01–C-03 | 2007、2025、2019 ※ |
| C2 錐形震波的結構與干涉 | C-04–C-06 | 2001 → 2022 |

所以 A-12 是 1964 年並不是編號錯亂，而是凹槽流場的文獻本來就從 1960 年代起步。

※ C1 是唯一組內年代非遞增的一組（C-01 2007、C-02 2025、C-03 2019）。C-03 是後來補進來的，
依本節規則接在該組號碼後面，而不是照年代插到 C-02 前面。

新增文獻時：**接在該主題那一組的號碼後面**，不要只看年代就往前插。如果新文獻不屬於
現有任何一組，就新開一組並補上 `/* --- 分組標題 --- */` 註解。
B 軌道原本的 Busemann 1942 那筆移除後留下空號，已把後面的號碼整體往前挪，
所以 B-01 到 B-19 現在是連號的，不再有空號。

---

## 目前的待辦（不要擅自改成 verified）

現況：**51 筆**（48 篇文獻 ＋ 3 本參考書），其中 41 筆 `verified`、4 筆 `conflict`、6 筆 `unverified`。

### `unverified`：六筆（全部在軌道 C）

A-01、A-16、A-17 已依出版社排版版 PDF 逐字核對後升級為 `verified`。
A-14 的引用衝突已由使用者依原報告裁定：本筆為 AFFDL-TR-74-133，不與 AIAA Paper 75-491 併筆，已升為 `verified`。
Busemann 1942 原文書目無法查證，且內收縮流場已由 B-11、B-12 涵蓋，整筆移除，
其後的 B 軌道號碼已整體往前挪一號補上空缺。

- **C-01** Ramanah et al. (2007)：卷期頁依 NASA ADS bibcode `2007ShWav..17...65R` 與 Springer 條目
  交叉比對填入，兩者一致，但未開啟出版社頁面逐字核對
- **C-02** Cooke et al. (2025)：同上，依 ADS bibcode `2025MeScT..36h5208C` 與 IOPscience 條目
- **C-03** Saiprakash et al. (2019)：六位作者與卷期頁依 jafmonline.net 期刊條目填入
- **C-04** Ben-Dor et al. (2001)：卷期頁 448, 147–174 由兩次獨立檢索交叉比對取得；標題副標亦來自檢索記錄
- **C-05** Skews et al. (2002)：共同作者依檢索補齊，Skews 的名字縮寫沿用使用者提供的寫法
- **C-06** Mölder & Timofeev (2022)：依 Springer 條目填入，兩次檢索一致

這六筆的 `authors` 都未經原文核對，取得 PDF 後請一併確認拼寫與順序。

### `conflict`：四筆

這四筆**不是查不到，是不同來源記載互相衝突**，需要判定採用哪一版。
卡片的 `note` 已寫明兩種說法，取得原文後即可裁定：

- **B-01** Taylor & Maccoll (1933)：頁碼 278–297（Royal Society 官方，Part II 另計 298–311）
  vs 278–311（部分二次文獻把兩部分合併）
- **B-03** Maccoll (1937)：年份 1937（DOI `rspa.1937.0083` 支持）vs 1936, 159(898)（JFM 2024 參考文獻）
- **B-12** Mölder (1967)：頁碼 1252–1255（多數來源且交叉一致）vs 1348–1355（另一份 AIAA 參考文獻）
- **B-13** Rylov (1990)：頁碼 54(2), 201–203（多數來源，本站採用）vs 245–249（另有來源）

### `pages`

48 篇文獻中已完成 **3 筆**（A-01、A-12、A-17），其餘 45 筆仍為空。

---

要把 `unverified` 或 `conflict` 升成 `verified`，**必須實際查證出版社頁面或出版社排版版 PDF，
並在回覆中說明查證來源**。搜尋引擎摘要、二次文獻轉述都不算數——摘要會把卷期頁講得很肯定，
但那是模型生成的轉述，不是查證。

---

## 工作方式

- 動 `LIBRARY` 以外的地方（版面、配色、程式邏輯）之前先說明你要改什麼，等確認再動
- 每次 commit 只做一件事，訊息用英文簡述，例如 `add A-24`、`verify biblio for A-01`
- 不要主動 push 到 `main`。除非使用者明講，否則開分支
- 使用者是流體力學研究生，不是前端工程師。解釋時說結果和影響，不要說 CSS 選擇器細節
- 回答用繁體中文、台灣用語

---

## 部署

GitHub Pages，`main` 分支根目錄。push 後約 1–2 分鐘生效。
網址：`https://p46144073-art.github.io/hsflow-lit/`
